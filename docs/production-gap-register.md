# Production Gap Register

This file records gaps that block or weaken a real private cross-border deployment. Items here are not optional decoration. They must be closed or explicitly accepted before a customer production handoff.

## P0 blocking

1. `media-service` now has upload/delete audit, 480/960/1600 WebP variants, Catalog alt/sort binding, deterministic 4xx compensation, post-unbind cleanup, PostgreSQL-backed uncertain-5xx reconciliation, failed-task admin retry/discard, GIF-to-H.264-MP4 conversion, and MP4 poster/duration extraction. PostgreSQL manual-action, FFmpeg/MinIO, and multi-image/alt operator drills passed; remaining media work is periodic drill automation and production HTTPS CDN verification.
2. DLQ is operational in admin at first usable level: list dead-letter tasks, show failure count and last error, retry, discard, handler, decision note, and audit trail. PostgreSQL-backed compensation failure drill passed on the test server on 2026-06-17; production handoff still needs periodic drill automation and alerting.
3. Admin order operations now include order detail, payment confirm/cancel in mock mode, exception handling, manual compensation, action audit, and real refund balance/history with partial/full refund controls. PostgreSQL-backed compensation, refund, and asynchronous refund-webhook drills passed on the test server. Production handoff still needs valid Sandbox approval/capture/refund, real signed and duplicate webhook E2E, role-gated refund authorization, and Live credential/certificate acceptance.
4. Admin inventory operations now include reservation ledger, confirm/cancel status lookup, human-readable stock fields, manual release, stock adjustment, stocktaking-style available quantity setting, safety-stock updates, low-stock indicators, and inventory action audit at first usable level. PostgreSQL failure drill, batch stocktaking, formal alert rules, and aftersales locked-stock writes are still required before production handoff.
5. Product admin must finish remaining cross-border operations: bulk validation and scheduled status controls. Per-asset bilingual alt, deterministic multi-image ordering, catalog/media binding compensation, HS Code, material composition, origin country, capacity, package dimensions, weight, customs declaration notes, bilingual detail content, and real initialization now have first usable admin/service fields.
6. Category, region, and product admin pages now initialize from `admin-gateway` real catalog APIs at first usable level. Static fallback is allowed only as an explicit local-development fallback and must not be cached.
7. `worker-service` compensation retry and DLQ insertion have been verified against PostgreSQL on the test server. Remaining work is to automate periodic drill execution, wire alerts, and repeat the same pattern for real payment/logistics/email providers.
8. Catalog Redis invalidation now has code-level policy coverage and a PostgreSQL/Redis live write-read drill for category, region, and product writes on the test server. Production handoff still needs this drill wired into periodic verification and alerting.
9. Unified business error codes now exist as a first usable package and gateway normalizer. Core commerce services, auth flows, operations actions, product import, payment mock flow, compensation worker/DLQ APIs, both gateways, and the primary storefront/admin error surfaces have first-usable standard payload coverage. Checkout and inventory reservation idempotency conflicts now have code-level `409 IDEMPOTENCY_CONFLICT` coverage. `support-service` and `store-service` currently expose no failing business branches. Remaining work includes PostgreSQL concurrency/runtime drills plus explicit real-provider and compensation-pending tests.
10. Transactional email is restored at first usable level, but production email delivery still needs the real Tencent SES/account-pool provider, provider health/failover, quota counters, recipient throttling persistence, admin audit coverage, and the full six-scene automation loop before production handoff: registration verification, registration success, order confirmation, shipping notice, refund notice, and order cancellation.
11. Logistics tracking is restored at first usable level, but real 17TRACK/TrackingMore/Ship24 adapters, durable quota reset jobs, external timeout/failover tests, anti-abuse request limiting, and admin audit coverage are still required before production handoff.
12. Product reviews are restored at first usable level, but production review handling still needs real order-purchase verification, media-service image upload binding, persistent IP/account throttling, CSRF/reCAPTCHA, abuse-word filtering, bulk moderation, and admin audit coverage before production handoff.
13. Legal/compliance storefront pages are restored at first usable level, but production handoff still needs backend-managed legal content, real store contact placeholders, cookie consent controls, and final legal review by the merchant before payment-provider submission.
14. Storefront preference and consent components are restored at first usable level, but production handoff still needs backend-managed market/currency rules, GeoIP defaulting, third-party script blocking tied to consent, and address/phone validation providers.
15. Operations management is restored at first usable level, including EdgeOne free certificate / Let's Encrypt / Cloudflare provider configuration, company credential fields, manual expiry reminder scans, and a real EdgeOne HTTPS/DNS/certificate/mixed-content check. Production handoff still needs real EdgeOne certificate application/deployment, Let's Encrypt automation, Cloudflare/EdgeOne cache/rules API integration, GA4/GSC verification, scheduled alert jobs, and operator permission/audit hardening.
16. Product import and AI workflow is restored at first usable level and now publishes validated drafts through the real catalog-service product write API before marking tasks published. Production handoff still needs real source-site fetchers, AI copy/image provider adapters, media-service image localization, queue workers, inventory initialization, and content risk checks.
17. Company compliance credential management now has first-usable admin fields and manual expiry reminder scans. Production handoff still needs real media attachment upload/binding, scheduled daily scans, and provider-backed reminder delivery verification.

## P0 closure matrix

| Dimension | Must close | Impact |
| --- | --- | --- |
| Business loop | PostgreSQL-verified worker compensation, batch inventory operations, formal stock alerts, and complete cross-border product operations | Operators cannot manage the full order and inventory lifecycle |
| Data consistency | Catalog admin -> storefront real-data loop and cache invalidation must be smoke-tested through gateways for category, region, and product writes | Storefront may read stale or non-authoritative admin data |
| Media capability | Automate periodic reconciliation/media drills and verify the production HTTPS CDN path | Product images, short videos, multi-image ordering, bilingual alt, and cleanup have test-server proof; recurring verification and production CDN acceptance are incomplete |
| Audit and compliance | Order actions, inventory actions, product writes, and configuration writes must create audit events; DLQ action audit and the unified admin audit viewer are first-usable complete | Production incidents cannot be traced to an operator or decision |
| Failure drills | Repeat and automate provider failure drills for inventory/payment/logistics/email; add alerts for compensation backlog and DLQ growth | PostgreSQL-backed inventory compensation failure is proven on the test server; broader provider drills remain open |
| Error system | Unified business error codes and user-facing copy must replace random service `message` strings across all core services | Frontend and admin cannot reliably distinguish validation, inventory, provider, or system failures |
| Notification | Real SES provider/account pool, durable quota counters, throttling, template audit, six-scene automation, and send-point verification must be completed | Transactional emails have templates and service boundaries, but production delivery and several triggers remain provider-incomplete |
| Logistics | Real provider adapters, durable quota resets, request throttling, failure drills, and tracking-email verification must be completed | Tracking UI and cache exist, but production logistics data is not yet connected to live aggregators |
| Reviews | Order-purchase verification, media upload binding, persistent throttling, anti-spam controls, moderation audit, and bulk actions must be completed | Review display and moderation exist, but production anti-abuse and proof-of-purchase controls are incomplete |
| Legal pages | Backend-managed policy content, real merchant details, cookie consent controls, and legal review must be completed | PayPal-facing policy routes exist, but customer-specific legal content is not yet configured |
| Storefront preferences | GeoIP market defaulting, backend market/currency configuration, consent-script gating, and phone/address validation must be completed | Cookie, market, and phone UI exist, but production data controls are not wired |
| Operations | Real EdgeOne free certificate application/deployment, Let's Encrypt renewal, CDN cache purge, analytics verification, scheduled alert emails, and role-gated operations must be completed | Admin can save EdgeOne/Let's Encrypt/Cloudflare provider config, run real DNS/HTTPS/certificate/mixed-content checks, record actions, maintain credential metadata, and trigger manual expiry scans, but real cloud execution is not connected |
| Company credentials | Media attachment upload/binding, scheduled expiry scans, and provider-backed reminder delivery verification must be completed | Merchant compliance evidence can be recorded at first usable level, but production reminders and file evidence lifecycle remain incomplete |
| Product import | Real crawler/fetcher adapters, AI copy/image providers, media localization, queue execution, inventory initialization, and content risk checks must be completed | Admin can import links, edit drafts, save AI config, and publish validated drafts into catalog-service; real AI/crawler/media execution and sellable inventory creation are not connected |

## P1 reliability and security

1. Health checks must distinguish process liveness from dependency readiness for PostgreSQL, Redis, MinIO/R2/S3, and critical downstream services.
2. Admin login must include brute-force protection: failure count, temporary lockout, and optional captcha/verification challenge.
3. Production admin access should support VPN/IP allowlist. If disabled, the customer must explicitly accept the risk in the runbook.
4. All admin write actions must enforce idempotency keys and audit logs with actor, IP, correlation ID, old value, new value, and affected business ID.
5. PUT and PATCH semantics must be separated. Full updates must validate and send the full object; partial updates must not clear omitted fields.
6. Gateways must propagate `x-correlation-id`, language, client type, auth, user-agent, and idempotency headers in every service call.
7. Logs for slow requests, compensation failures, DLQ actions, provider failures, and admin writes must be structured JSON.
8. Slow request thresholds:
   - order creation: `SLOW_ORDER_CREATE_MS`, default 2000 ms
   - inventory reservation: `SLOW_INVENTORY_RESERVE_MS`, default 1000 ms
   - catalog read: `SLOW_CATALOG_READ_MS`, default 500 ms
9. Alerts must be added for inventory below safety stock, order creation error rate, compensation backlog, DLQ growth, and upload failure rate.

## P2 scale and delivery

1. Growable endpoints must paginate consistently. Use one pagination style per API version and document it.
2. OpenAPI contracts must be generated or checked against service DTOs before integration.
3. Database migrations should be grouped by service ownership and include rollback or recovery instructions.
4. Backup and restore scripts must cover PostgreSQL, object storage, and exported customer configuration.
5. Environment files must be layered by deployment target and validated at service startup.
6. API versioning must be defined before external customer integrations depend on endpoints.
7. ADRs must record load-bearing decisions such as TCC/Saga, Redis caching, Docker Compose first, managed state services, and Provider interfaces.
8. Local verification and deployment configuration scripts are restored, but production-specific `.env.prod` validation must be run for every customer handoff.

## Current closure status

- Homepage CMS: first usable end-to-end implementation is complete through `store-service`, both gateways, admin and storefront. Layout modules persist in PostgreSQL; admin supports ordering, visibility, duplication, deletion confirmation, bilingual content, links/items, media upload, publish confirmation, connectivity testing and dual-device preview. The homepage collection now resolves three real Catalog products with SKU/inventory/cross-border detail data. Newsletter operations include paginated list/search/status filters, CSV export, confirmed unsubscribe/reactivation, operator timestamps and event audit. Local API and 1920/375 Playwright verification passed. Production still needs deployment, HTTPS-domain preview validation, real campaign delivery through the notification provider, and production-scale subscriber/export acceptance.
- Media upload: implemented through `media-service`, `admin-gateway`, and admin product UI. Main objects, variants, and posters are persisted as origin-free `/media/public/{objectKey}` references; storefront/admin proxy that path to media-service, while external sources require explicit HTTPS. Database migration and constraints prevent new HTTP media/template/review references. Test-server FFmpeg/MinIO and same-origin proxy verification passed with correlation ID `media-processing-drill-c757876e-cddc-4ed3-a656-c8ab7c6dc163`; multi-image order, bilingual alt, compensation, reconciliation, and cleanup retain prior proof. Remaining media work is periodic drill automation and production HTTPS domain/CDN acceptance; the current HTTP test-server proxy is not production TLS evidence.
- Catalog admin initialization and cache policy: category, region, and product admin pages now read real catalog data through `admin-gateway`. Product admin uses the backend-only `catalog-service GET /admin/products` endpoint so inactive/draft products and cross-border fields are visible to operators. Catalog cache invalidation policy is now extracted into `cache-policy.ts` with tests proving category/region writes also clear dependent product summaries, storefront product projections, and storefront aggregate cache keys. `scripts/run-server-catalog-cache-drill.ps1` passed on 2026-06-17 against the test server by preheating Redis, updating/restoring category `teapot`, region `beijing`, and product SKU `TEA-PORCELAIN-SET-001` through `admin-gateway`, verifying dependent keys were deleted, and verifying `api-gateway /catalog/storefront` returned the updated values before restore. Correlation ID: `cache-drill-1781696871`.
- DLQ admin list/retry/discard/audit trail: implemented at first usable level through `worker-service`, `admin-gateway`, PostgreSQL `dead_letter_audit_events`, and admin UI. PostgreSQL-backed compensation failure drill passed on 2026-06-17 against the test server: order `ef3a0586-7da9-458c-a219-6d9b1d7dfbdb`, correlation ID `server-drill-7200ffbe-f201-4e19-a11c-5fc5b5087b56`, one open DLQ row verified through `admin-gateway /dead-letter-tasks`.
- Admin order operations: order list, exception red tag, latest failure reason, order detail, order line snapshots, Mock payment confirm/cancel, manual inventory compensation requeue, and order action audit trail are implemented at first usable level through `order-service`, `admin-gateway`, PostgreSQL `order_audit_events`, and admin UI. PostgreSQL inventory compensation failure drill passed on the test server; real provider failure drills are still pending.
- Guest checkout post-registration: checkout success now exposes a real account-creation form tied to the order email, calls `auth-service /register`, and sends the existing verification email. The account order-history page now reads `api-gateway -> order-service /orders/customer-history` by signed-in email and no longer shows static fake orders. Production still needs durable customer profile/address storage, stronger auth/session enforcement around customer order reads, and verified order-history E2E with PostgreSQL.
- Inventory operations: human-readable fields, reservation ledger, confirm/cancel status lookup, manual release for reserved reservations, stock adjustment, stocktaking-style available quantity setting, safety-stock update, low-stock indicator, and inventory action audit are implemented at first usable level through `inventory-service`, `admin-gateway`, PostgreSQL `inventory_audit_events`, and admin UI. PostgreSQL failure drill, batch stocktaking, formal alert rules, and aftersales locked-stock writes are still pending.
- Slow request structured logs: implemented for order create, inventory reserve, and catalog reads. Grafana alert rules are still pending.
- Compensation failure drill: `scripts/run-compensation-drill.ps1`, `scripts/run-server-compensation-drill.ps1`, and Runbook steps are implemented. The local Docker daemon is still unavailable on the workstation, but the server drill passed on 2026-06-17 and proves order payment confirmation can queue an inventory compensation task, worker retry can dead-letter it, and admin-gateway can read the DLQ task.
- Transactional notification: `notification-service` has been restored with PostgreSQL-backed templates/logs, admin template editing, auth registration/password emails, payment success emails, and review invitation emails. The requested six-scene automation now has first-usable template coverage: registration verification, registration success, order confirmation, shipping notice, refund notice, and order cancellation. The service also includes the `company_credential_expiry` template for operations reminders. Admin email settings now show English HTML preview, enable/disable editing, real send logs/failure records, a persisted verification-link expiry policy, and a PostgreSQL-backed email account pool with daily quota counters. `auth-service` uses `email_settings.verification_token_ttl_minutes` when creating verification tokens and passes `{{expiresInMinutes}}` into the registration template. Order creation sends `order_confirmation`, and logistics update emails use `shipping_notice`. Production still needs stronger verification security policy controls, refund/cancel real business triggers, template publish/audit workflow, backend tutorial coverage, real Tencent SES API delivery, provider health/failover, and end-to-end SMTP/API sandbox verification.
- Logistics tracking: `logistics-service` has been restored with API-account pool shape, tracking cache, call logs, Mock Provider, admin account/log panel, storefront `/track-order` page, gateway routes, and logistics-update email send endpoint. Real 17TRACK/TrackingMore/Ship24 adapters, monthly quota reset, external provider circuit breaking, and anti-abuse throttling are still pending.
- Product reviews: `review-service` has been restored with PostgreSQL-backed pending/approved/hidden/deleted reviews, storefront product review display/submission, admin moderation/reply/pin controls, gateway routes, and pending-review admin email notification. Review submission now verifies the submitted order through `order-service`, requires the customer email to match, requires `paymentStatus = paid`, and rejects orders that do not contain the reviewed product. Production still needs delivered-order delayed invitation scheduling, media upload binding for review photos, persistent throttling, CSRF/reCAPTCHA, abuse-word filtering, rejection retention, bulk moderation, and moderation audit logs.
- Error system: `@commerce/error-codes` has been restored with standard codes, default messages, HTTP-status mapping, gateway error payload normalization, and shared Chinese/English client copy rendering. `inventory-service`, `order-service`, `catalog-service`, `review-service`, `logistics-service`, `notification-service`, `media-service`, `payment-service`, `worker-service`, `auth-service`, `ops-service`, and `product-import-service` now return standard payloads for their primary validation, state, authentication, authorization, missing-resource, provider/dependency, operations-action, and DLQ failures. Catalog missing category/region references now return HTTP 404 with `NOT_FOUND`, verified through the server admin gateway. Unknown operations actions now fail explicitly with `400 VALIDATION_FAILED` instead of returning `accepted: false` under a successful HTTP response. Checkout now stores a SHA-256 normalized-request fingerprint and rejects a reused key with changed email, address, item, price, or quantity as `409 IDEMPOTENCY_CONFLICT`; inventory reservation replay validates SKU, warehouse, and quantity under the same code. Both gateways preserve downstream business error status/payloads and convert connection/DNS/network failures into `503 DEPENDENCY_UNAVAILABLE` with the actual forwarded correlation ID; this behavior was runtime-verified against unreachable Catalog endpoints. `support-service` and `store-service` currently expose only health/default-store reads and have no failing business branches to migrate. Storefront checkout, account/password, registration, review, and tracking flows render messages in the current storefront language. Admin order, inventory, logistics, review, product media, email settings, DLQ, operations settings, product import, category, and region flows render shared Chinese messages. True concurrent race drills and explicit real-provider/remaining-compensation tests are still required before this gap can be closed.
- Storefront legal and payment result pages: `/privacy-policy`, `/refund-return-policy`, `/terms-of-service`, `/contact-us`, `/payment-result`, and the shared storefront footer have been restored using the premium minimal visual system. The content uses merchant placeholders and must be connected to backend site settings before customer handoff.
- Storefront preference/consent UI: cookie consent, international phone field, market preference selector, line-art teaware loading overlay, and simplified language toggle have been restored. Cookie category management, third-party script gating, GeoIP defaults, backend market rules, and phone/address validation are still pending.
- Operations service: `ops-service` has been restored with PostgreSQL-backed SSL/CDN/GA4/GSC settings, EdgeOne free certificate / Let's Encrypt / Cloudflare provider configuration, real EdgeOne HTTPS/DNS/certificate/mixed-content checks, company credential metadata, manual credential-expiry scans, audit events, action recording, admin-gateway routes, Docker Compose wiring, and an admin operations panel. Real EdgeOne certificate application/deployment, Let's Encrypt renewal, Cloudflare/EdgeOne cache purge/rules API, analytics verification, scheduled alert jobs, media-backed credential attachments, and RBAC/IP allowlist enforcement are still pending.
- Product import workflow: `product-import-service` has been restored with PostgreSQL-backed AI configuration, link import tasks, editable draft records, publish-field validation, audit events, admin-gateway routes, Docker Compose wiring, and an admin product import panel. Publishing now calls `catalog-service PUT /products` with integer `priceMinor`, waits for the matching SKU response, and only then marks the import task published; catalog rejection or outage leaves the task unpublished. Real crawler/fetcher adapters, AI copy/image adapters, media-service image localization, asynchronous queue workers, inventory initialization, and risk checks are still pending.
- Unified audit viewer: the admin `审计日志` page has been restored and aggregates real inventory, catalog, media, operations, and product-import audit sources through `admin-gateway`. Catalog category/region/product writes now emit `catalog_audit_events` with actor, old/new values, summary, and correlation ID. Order detail and DLQ audit trails remain module-local, and review/logistics/notification moderation or send-operation audits still need broader coverage before production handoff.
- E2E coverage: Playwright smoke coverage now includes restored legal routes, payment result, tracking, product reviews, support widget placement, admin email settings, and restored admin logistics/reviews/ops/product-import/email-template/audit panels. The targeted restored-surface E2E suite passed on 2026-06-15. Full provider-backed E2E remains pending until real external adapters and Docker/server dependencies are available.
- Delivery guardrails: `scripts/run-local-verification.ps1`, `scripts/validate-deployment-config.ps1`, `docs/product-import-ai-workflow.md`, and `docs/ui-interaction-templates.md` have been restored. Customer-specific production environment validation still must be run against real, non-placeholder secrets and service endpoints.
- Local dev orchestration: root `pnpm dev` now sets `--concurrency=18` so every long-running app/service in the local stack starts instead of stalling after Turbo's default concurrency window. This is required for Playwright webServer and local full-stack smoke checks.
